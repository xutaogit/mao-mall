# 猫商城项目开发规范 v2.0

## 项目架构
- **后端：** Node.js + Express + MongoDB (端口3000)
- **H5商城：** Vue3 + Vite + Vant 4.x (端口5173)
- **管理后台：** Vue3 + Vite + Element Plus (端口5174)
- **部署：** Vercel + MongoDB Atlas
- **域名：** m.maomall.top (H5) / admin.maomall.top (后台) / api.maomall.top (API)

## 技术栈版本（严格遵守）

### 后端
- Node.js: >=18.x (Vercel 使用 20.x)
- Express: 4.18.2
- Mongoose: 8.0.3
- JWT: jsonwebtoken 9.0.3
- 密码加密: bcryptjs 3.0.3

### 前端 H5
- Vue: 3.4.0
- Vite: 5.0.0
- **Vant: 4.8.0** ⚠️ 重要：4.x 组件名称有变化
- Vue Router: 4.2.5
- Axios: 1.6.0

### 管理后台
- Vue: 3.4.0
- Element Plus: 2.5.0
- 其他同前端 H5

## ⚠️ 关键注意事项（必读）

### 1. Vant 4.x 组件名称变更（高频错误）

| Vant 3.x | Vant 4.x | 说明 |
|----------|----------|------|
| van-goods-action | van-action-bar | 商品操作栏 |
| van-goods-action-icon | van-action-bar-icon | 操作栏图标 |
| van-goods-action-button | van-action-bar-button | 操作栏按钮 |

✅ 正确写法：
```vue
<van-action-bar>
  <van-action-bar-icon icon="chat-o" text="客服" />
  <van-action-bar-button type="danger" text="立即购买" />
</van-action-bar>
```

❌ 错误写法（不要使用）：
```vue
<van-goods-action>
  <van-goods-action-icon icon="chat-o" />
</van-goods-action>
```

### 2. 后端响应格式（统一标准）

所有 API 必须返回：
```javascript
// 成功
{ success: true, data: {}, message: "成功" }

// 失败
{ success: false, message: "错误信息" }
```

前端响应拦截器直接返回 `response.data`，不检查 `res.code`

### 3. MongoDB ObjectId 验证（防止 500 错误）

查询前必须验证 ObjectId 格式：
```javascript
// ✅ 正确
if (categoryId && categoryId.match(/^[0-9a-fA-F]{24}$/)) {
  query.categoryId = categoryId
}

// ❌ 错误：直接使用会导致 Cast 错误
query.categoryId = categoryId
```

### 4. Vercel Serverless 配置

后端 index.js 必须：
```javascript
// ✅ 正确
if (process.env.VERCEL !== '1') {
  app.listen(PORT)
}
export default app

// ❌ 错误：Vercel 环境下会失败
app.listen(PORT)
```

### 5. 环境变量

**后端（Vercel 面板配置）：**
```
MONGODB_URI=mongodb+srv://...
MONGODB_DB_NAME=mao-mall
JWT_SECRET=your-secret
ALLOWED_ORIGINS=https://admin.maomall.top,https://m.maomall.top
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
```

**前端（Vercel 面板配置）：**
```
VITE_API_URL=https://api.maomall.top/api
```

## 项目结构

### 后端 (backend/)
```
src/
├── config/database.js          # MongoDB 连接
├── middleware/auth.js          # JWT 认证
├── models/                     # 数据模型
│   ├── Member.js               # 用户
│   ├── Product.js              # 商品
│   ├── ProductSku.js           # SKU
│   ├── ProductCommission.js    # 商品佣金
│   ├── Order.js                # 订单
│   ├── OrderRefund.js          # 退款
│   ├── Payment.js              # 支付记录
│   ├── CartItem.js             # 购物车
│   ├── Category.js             # 分类
│   ├── Coupon.js               # 优惠券
│   ├── Distributor.js          # 分销员
│   ├── DistributionOrder.js    # 分销订单
│   └── DistributionRelation.js # 分销关系
├── routes/                     # API 路由
│   ├── auth.js                 # 认证
│   ├── products.js             # 商品
│   ├── sku.js                  # SKU
│   ├── orders.js               # 订单
│   ├── cart.js                 # 购物车
│   ├── addresses.js            # 地址
│   ├── coupon.js               # 优惠券
│   ├── refund.js               # 退款
│   ├── payment.js              # 支付
│   ├── distributor.js          # 分销（用户端）
│   └── distributorAdmin.js     # 分销（管理端）
└── index.js                    # 入口
```

### 前端 H5 (frontend/)
```
src/
├── api/                        # API 封装
├── views/                      # 页面组件
│   ├── Home.vue                # 首页
│   ├── ProductDetail.vue       # 商品详情
│   ├── Cart.vue                # 购物车
│   ├── OrderConfirm.vue        # 订单确认
│   ├── Payment.vue             # 支付页面
│   ├── User.vue                # 个人中心
│   ├── DistributorApply.vue    # 分销申请
│   └── DistributorCenter.vue   # 分销中心
├── router/index.js             # 路由配置
└── utils/request.js            # Axios 封装
```

### 管理后台 (admin/)
```
src/
├── api/                        # API 封装
├── views/
│   ├── Login.vue               # 登录
│   ├── Products.vue            # 商品列表
│   ├── ProductEdit.vue         # 商品编辑（含SKU）
│   ├── Orders.vue              # 订单管理
│   ├── Refunds.vue             # 退款管理
│   └── Coupons.vue             # 优惠券管理
├── router/index.js
└── utils/request.js
```

## 核心功能模块

1. **商品管理** - CRUD、SKU、分类、佣金配置
2. **订单管理** - 创建、支付（模拟）、发货、退款
3. **用户系统** - 注册登录、地址、个人中心
4. **购物车** - 加购、数量调整、结算
5. **优惠券** - 创建、领取、使用（满减/折扣/无门槛）
6. **分销系统** - 申请审核、推广链接、佣金结算
7. **支付系统** - 支付单据、模拟支付（待接入真实支付）

## 认证机制

### JWT Token
- **用户端：** `localStorage.getItem('token')`
- **管理端：** `localStorage.getItem('adminToken')`
- **请求头：** `Authorization: Bearer ${token}`
- **过期时间：** 7天

### 路由守卫
```javascript
// H5 前端
router.beforeEach((to, from, next) => {
  const token = localStorage.getItem('token')
  if (to.meta.requiresAuth && !token) {
    next('/login')
  } else {
    next()
  }
})

// 管理后台
router.beforeEach((to, from, next) => {
  const adminToken = localStorage.getItem('adminToken')
  if (to.path !== '/login' && !adminToken) {
    next('/login')
  } else {
    next()
  }
})
```

## 数据库规范

### 字段命名
- ✅ 驼峰命名：`userId`, `productName`, `createdAt`
- ❌ 下划线：~~`user_id`~~, ~~`product_name`~~

### 通用字段
```javascript
{
  createdAt: Date,      // 自动
  updatedAt: Date,      // 自动
  deleteStatus: {       // 软删除
    type: Number,
    default: 0          // 0-未删除 1-已删除
  }
}
```

### 状态枚举
- **订单状态：** 0-待付款 1-待发货 2-已发货 3-已完成 4-已关闭
- **退款状态：** 0-待审核 1-审核通过 2-审核拒绝 3-已完成
- **支付状态：** 0-待支付 1-已支付 2-已取消 3-已退款
- **分销员状态：** 0-待审核 1-审核通过 2-审核拒绝

## API 接口规范

### RESTful 设计
- GET `/api/products` - 获取列表
- GET `/api/products/:id` - 获取详情
- POST `/api/products` - 创建
- PUT `/api/products/:id` - 更新
- DELETE `/api/products/:id` - 删除
- PATCH `/api/products/:id/publish` - 部分更新

### 分页参数
```javascript
// 请求
{ page: 1, pageSize: 10, keyword: '', categoryId: '' }

// 响应
{
  success: true,
  data: {
    list: [],
    total: 100,
    page: 1,
    pageSize: 10,
    totalPages: 10
  }
}
```

## Vue 3 组件规范

```vue
<template>
  <!-- 模板 -->
</template>

<script setup>
// 1. 导入
import { ref, reactive, computed, onMounted } from 'vue'
import { useRouter } from 'vue-router'

// 2. 响应式数据
const loading = ref(false)
const form = reactive({})

// 3. 计算属性
const total = computed(() => {})

// 4. 方法
const handleSubmit = async () => {}

// 5. 生命周期
onMounted(() => {})
</script>

<style scoped>
/* 样式 */
</style>
```

## 常见问题

### 1. Vant 组件不显示
**原因：** 使用了 Vant 3.x 组件名  
**解决：** 改用 Vant 4.x 组件名（见上文）

### 2. Cast to ObjectId failed
**原因：** 传入非法 ObjectId  
**解决：** 查询前验证格式

### 3. Vercel 部署失败
**检查：**
- Node.js 版本（20.x）
- 环境变量是否配置
- Root Directory 是否正确

### 4. CORS 跨域
**解决：** 更新后端 `ALLOWED_ORIGINS` 环境变量

### 5. 前端请求超时
**检查：** `vite.config.js` 代理端口是否与后端一致

## Git 提交规范

```bash
feat(product): 添加SKU管理功能
fix(cart): 修复数量更新问题
docs(readme): 更新部署说明
```

类型：feat, fix, docs, style, refactor, perf, test, chore

## 部署流程

1. 推送代码：`git push`
2. Vercel 自动部署（1-2分钟）
3. 更新环境变量需手动 Redeploy

## 参考文档

- Vant 4.x: https://vant-ui.github.io/vant/v4/
- Element Plus: https://element-plus.org/
- Mongoose: https://mongoosejs.com/

---

**版本：** v2.0.0  
**更新：** 2026-03-14
